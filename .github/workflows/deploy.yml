name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v2

    - name: Print FTP Host
      run: echo "FTP_Host:${{ secrets.FTP_HOST }}"

    - name: Test DNS Resolution
      run: nslookup ${{ secrets.FTP_HOST }} 8.8.8.8

    - name: Test FTP Connectivity
      run: |
        echo "Open FTP connection to ${{ secrets.FTP_HOST }}"
        # Install ftp command if not present
        sudo apt-get update && sudo apt-get install -y ftp
        ftp -inv ${{ secrets.FTP_HOST }} <<EOF
        bye
        EOF

    - name: Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.3
      with:
        server: ${{ secrets.FTP_HOST }}
        username: ${{ secrets.FTP_USER }}
        password: ${{ secrets.FTP_PASS }}
        server-dir: public_html/
        log-level: verbose
        dry-run: true
